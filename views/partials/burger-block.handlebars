<li>
	{{id}}
	{{burger_name}}
	{{#if devour}}

	<button class="change-devour float-right" data-id="{{id}}" data-newdevour="{{devour}}">
		Devour It!
	</button>
	{{else}}
	<button class="delete-burger float-right" data-id="{{this.id}}">Discard!</button>
	{{/if}}
</li>